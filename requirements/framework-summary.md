# Framework Summary

This repository scaffolds a sector-relative stock selection framework with typed contracts, config-driven weights, and explicit penalty traces.

The intent is to help Codex implement the framework in small deterministic milestones rather than generating an opaque screener in one pass.
